---
name: design-tool-routing
description: Routes material Confecciones SV design work through shipped references when useful, divergent exploration only when unresolved, project-owned art direction, anti-AI-slop craft checks and exact-head visual QA.
---

# Confecciones SV design tool routing

Authority: `PRODUCT_TRUTH + OWNER + PROJECT_VISUAL_DNA > EXTERNAL_REFERENCE_OR_TOOL`.

Use only when a material visual mission is explicitly active. This project is currently frozen/parked; adding this skill does not reactivate product work.

Flow when applicable:
`PRODUCT_TRUTH -> REAL_SHIPPED_REFERENCE -> DISTINCT_DIRECTIONS -> SELECTED_VISUAL_TARGET -> IMPLEMENTATION -> ANTI_SLOP_PRECHECK -> EXACT_HEAD_PIXEL_QA -> ART_DIRECTOR_POST -> OWNER_QA`.

- Mobbin/equivalent: mechanics, hierarchy, density, state treatment; `REFERENCE != STYLE_AUTHORITY`.
- Product Design/equivalent: only for unresolved material directions; `THREE_RECOLORS != THREE_DIRECTIONS`.
- Impeccable/equivalent: optional writer-side craft precheck (`audit/layout/typeset/distill/harden/polish`); `AI_SLOP_DETECTOR != DESIGN_AUTHORITY`.
- ChatGPT/Work may perform live research and persist findings; Gemini/Antigravity and Codex consume durable repo/Issue evidence.
- MCP/plugins are for live external data/actions, not static advice: `MCP_AVAILABLE != MCP_JUSTIFIED`.

If an external tool is unavailable, use `.agents/skills/art-direction/SKILL.md`; never invent references or block solely on vendor absence.
